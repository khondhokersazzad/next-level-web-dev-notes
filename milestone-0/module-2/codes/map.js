const course1 = {name : 'Programming Hero'}
const course2 = {name : 'Next Level '} // in obj we can use only string and symbol as key 

const map = new Map();

map.set(false, 'Level three web development'); // In map we can set any data type as key 

console.log(map);